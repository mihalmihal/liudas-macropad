#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/outputs.h>

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
            bindings = <
                &kp LALT(NUMBER_8) &kp LS(A) &kp LALT(NUMBER_9) &kp LALT(LCMD(K)) &kp LALT(LCMD(B)) &kp LSHIFT(LCMD(BACKSLASH)) &kp LALT(LCMD(C)) &kp LALT(LCMD(V)) &kp LSHIFT(LCMD(R))
            >;
        };
    };
};