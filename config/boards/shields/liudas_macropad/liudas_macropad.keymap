#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/outputs.h>

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
            bindings = <
                &kp LA(NUMBER_8) &kp LS(A) &kp LA(NUMBER_9) &kp LA(LCMD(K)) &kp LA(LCMD(B)) &kp LS(LCMD(BACKSLASH)) &kp G &kp H &kp I
            >;
        };
    };
};