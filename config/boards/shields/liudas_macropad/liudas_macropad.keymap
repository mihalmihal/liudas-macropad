#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/outputs.h>

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
            bindings = <
                &kp LC(C) &kp LC(V) &kp C &kp D &kp E &kp F &kp G &kp H &kp I &kp V &kp K &kp L &kp M &kp N &kp P &kp Q &kp R &kp S
            >;
        };
    };
};